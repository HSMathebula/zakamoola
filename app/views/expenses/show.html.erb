<p style="color: green"><%= notice %></p>

<%= render @expense %>

<div>
  <%= link_to "Edit this expense", edit_expenses_group_path(@expense) %> |
  <%= link_to "Back to expenses", expenses_group_path %>

  <%= button_to "Destroy this expense",  expenses_group_path, method: :delete %>
</div>
